<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<configuration>
    <!-- 导入外部的参数 -->
    <properties resource="jdbc.properties"></properties>

    <settings>
        <setting name="cacheEnabled" value="true"/> <!-- mybaits-config.xml中开启全局缓存（默认开启） -->
    </settings>
    <!-- 实体类别名 -->
    <typeAliases>
        <!--<typeAlias type="com.qf.entity.User" alias="user_shine"/>-->
        <!-- 定义实体类所在的package，每个实体类都会自动注册一个别名=类名 -->
        <package name="org.example.entity"/>
    </typeAliases>
    <!--  核心配置信息 -->
    <environments default="druid_config">
        <!-- 数据库相关配置 -->
        <environment id="shine_config">
            <!-- 事务控制类型 -->
            <transactionManager type="jdbc"></transactionManager>
            <!-- 数据连接参数  -->
            <dataSource type="org.apache.ibatis.datasource.pooled.PooledDataSourceFactory">
                <property name="driver" value="${jdbc.driver}"/>
                <!-- &转义&amp; -->
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>


        <!-- 数据库相关配置 -->
        <environment id="druid_config">
            <!-- 事务控制类型 -->
            <transactionManager type="jdbc"></transactionManager>
            <!-- 数据连接参数  -->
            <dataSource  type="org.example.util.DruidDSFactory" >
                <!--注意property 的属性和普通的 不一样-->
                <property name="driverClass" value="${jdbc.driver}"/>
                <property name="jdbcUrl" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>

    </environments>

    <!-- 注册mapper文件 -->
    <mappers>
        <mapper resource="mapper/UserDAOMapper.xml"/>
        <mapper resource="mapper/UserDAOMapper2.xml"/>
        <mapper resource="mapper/StudentDAOMapper.xml"/>
        <mapper resource="mapper/PassengerDAOMapper.xml"/>
        <mapper resource="mapper/PassportDAOMapper.xml"/>
        <mapper resource="mapper/DepartmentDAOMapper.xml"/>
        <mapper resource="mapper/EmployeeDAOMapper.xml"/>
        <mapper resource="mapper/SubjectDAOMapper.xml"/> <!-- -->
   </mappers>
</configuration>